在v1的基础上尝试着实现多个允许多个连接 并将数据同时发送给多个客户端，但是出现匪夷所思的bug

bug描述：sess->s1是一个全局的监听socket，所有的线程中的sess->s1都相同，客户端发送set_transport_type命令后给客户端建立一个线程
线程中调用accpet（sess->s1。。。）获得传输数据的socket，客户端刚开始连接的时候是正常的，可以发送数据，但是如果有一个链接断开 注销掉线程后，其他的线程发送数据就可能会失败！这可能的概率异常的高

上述bug可能是因为sess->s1是一个全局的socket 所以accpet（sess->s1...)返回的socket本地端口号都是一样的所以相互干扰，现在将sess->s1设置成独立的监听socket 但是由于客户端的杂乱命令连接不上真是纠结，不管怎么样快接近成功了


连接不上，因为客户端是连接指定的端口的，而独立的sess->s1的话，客户端本不来连接。。。。